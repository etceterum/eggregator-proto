module JournalHelper
  #def link_to_pin_story(text, journal, story, page)
  #  url = url_for(:action => 'pin_story', :story_id => story.id, :journal_id => journal.id, :page => page)
  #  "<a href='" + url + "' " +
  #  "onclick=\"" +
  #  "new Ajax.Request('" + url + "', { " +
  #  "method: 'POST', asynchronous: true, " +
  #  "onSuccess: agg.journal.onPinSuccess, onFailure: agg.journal.onPinFailure, onComplete: agg.journal.onPinComplete " +
  #  " }); return false;\">" + text + "</a>"
  #end
end
